'use strict';

const Player = require('./Player');
const PlayerManager = require('./PlayerManager');
const Lavalink = require('./Lavalink');

/** @typedef {Object} 
 * @prop {import('./Player')} Player
 * @prop {import('./PlayerManager')} PlayerManager
 * @prop {import('./Lavalink')} Lavalink
*/
module.exports = {
	Player,
	PlayerManager,
	Lavalink,
};
